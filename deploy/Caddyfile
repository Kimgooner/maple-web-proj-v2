# mapledelta.kr — 도메인만 적으면 Caddy 가 인증서를 받고 http → https 로 넘긴다.
mapledelta.kr, www.mapledelta.kr {
	encode zstd gzip

	# HSTS. Caddy 는 이걸 자동으로 붙이지 않는다. 한 번 https 로 온 브라우저는
	# 그다음부터 http 로 시도조차 하지 않는다.
	header Strict-Transport-Security "max-age=31536000; includeSubDomains"


	# 정적 파일과 /api 프록시는 nginx(web) 가 맡는다. Caddy 는 TLS 만 맡는다.
	# SSE 는 버퍼링 없이 흘려보낸다 (web 쪽 proxy_buffering off 와 짝이다).
	reverse_proxy web:80 {
		flush_interval -1
	}
}

# IP 로 직접 오는 요청. 전에는 여기서도 사이트를 통째로 서빙했는데, 그러면 TLS 없이
# 같은 화면이 하나 더 열려 있는 셈이라 HSTS 도 소용이 없다(브라우저는 IP 주소에
# HSTS 를 걸지 않는다). 살아 있는지만 알려주고 끝낸다 - 점검은 ssh 로 들어가
# `curl localhost` 로 한다.
http://140.245.77.108 {
	respond "ok" 200
}
